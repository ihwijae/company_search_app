## 미해결 사항 요약 (2025-01-05)

### 1. 가로 스크롤 이슈
- 협정보드 테이블 전체 폭이 화면보다 넓어도 스크롤바가 표시되지 않음.
- 원인: `src/index.css` 내부에 남아 있는 예전 협정보드 레이아웃 규칙이 `styles.css`를 덮어쓰기 때문.
- 조치 필요: `index.css`에서 협정보드 관련 블록 제거 또는 `styles.css`로 이관, import 순서 조정.

### 2. 헤더 디자인/크기 문제
- 기초금액~시공비율 카드가 협정보드디자인.png처럼 보이지 않음.
- 일정 폭 이상으로 줄여도 `index.css`의 구버전 스타일이 적용돼 헤더가 잘림.
- 조치 필요: 위 1번 조치 후 `styles.css`만 적용되는지 확인.

### 3. 공고번호/공고명 입력 영역
- 한 칸으로 축소하려 했으나, `index.css`에 남은 구규칙 때문에 기존 레이아웃이 계속 보임.
- 조치 필요: 동일.

## 향후 작업 계획
1. `src/index.css`에서 협정보드 관련 CSS 블록( `.agreement-board-root`, `.excel-board-shell`, `.excel-board-banner`, `.excel-amount-card`, `.excel-board-table` 등)을 삭제.
2. 협정보드 스타일은 `src/styles.css` 한 곳에서만 관리하도록 정리.
3. 정리 후 dev 서버 재시작 + 캐시 비우고, 헤더/스크롤 정상 동작 여부 확인.
4. 디자인 확인 후 필요 시 카드 크기/색상 미세 조정.
